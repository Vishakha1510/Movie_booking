import 'package:flutter/material.dart';

class MoviesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Movies')),
      body: Center(child: Text('Movies API Integration goes here')),
    );
  }
}
